global:
  scrape_interval: 5s  # 5초마다 메트릭 수집

scrape_configs:
  - job_name: 'spring-boot'
    static_configs:
      - targets: ['host.docker.internal:8080']  # 로컬 Spring Boot 서버
    metrics_path: '/actuator/prometheus'